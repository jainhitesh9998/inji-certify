# Rebuild progress log

Newest first. One entry per work package or notable finding. Branch names are on `jainhitesh9998/inji-certify`.

| When (UTC) | WP | State | Branch | Notes |
| --- | --- | --- | --- | --- |
| 2026-09-19 | P1-11a ConfigurationRegistry | done | `wp/p1-11a-configuration-registry` | `JpaConfigurationRegistry` reads `credential_config` rows as SPI `CredentialConfiguration`s (selectors per format, velocity `TemplateRef`, `keymanager:APPID/REFID` signing, strategy from plugin mode, Bitstring status purposes, display); read-only, active rows, single tenant. Unit tests over a mocked repository and a real-context check in the golden suite; full suite 889/889. Bridge for the `/oid4vci` adapter (P1-11) and the CLI `issue` command. |
| 2026-09-19 | P1-08 TemplateEngine (second slice: jsonmap) | done | `wp/p1-08-jsonmap` | New module `certify-template-jsonmap`: the no-code engine (JSON document with `${claim}` placeholders, typed substitution, nested paths, fallbacks, issuance built-ins, template params, `$claims` spread; nothing executed), auto-configured. Five tests. With Velocity (first slice) and `TemplateRef.Mode.NONE` in the core, the three templating options of the design exist behind one SPI. |
| 2026-09-19 | P1-09 ProofValidator (first slice) | done (first slice) | `wp/p1-09-proof-validator` | `JwtProofValidatorAdapter` implements the SPI `ProofValidator` over today's `JwtProofValidator`: nonce via the adapter's `NonceCheck`, allowed algorithms and client id from `ProofPolicy`, holder returned as the derived `did:jwk`/`did:key` `HolderBinding`, header violations named (`proof_header_invalid_typ`/`_alg`) instead of the legacy boolean. Five tests with real ES256 proofs; full suite 884/884. Legacy path untouched; `DefaultIssuanceService` can now be fed real proofs once the adapter surface (P1-11) exists. |
| 2026-09-19 | P1-08 TemplateEngine (first slice) | done (first slice) | `wp/p1-08-template-engine` | New module `certify-template-velocity`: `VelocityRenderer` (the exact Velocity 1.7 + tools setup), `VelocityTemplateEngine` implementing the SPI `TemplateEngine` for the new core (claims, `_issuer`, `_holderId`, validity, params, tools; JSON object out), auto-configured. The legacy `VCFormatter` (`VelocityTemplatingEngineImpl`) now evaluates through the shared renderer instead of its own engine; goldens unchanged, its 22 tests untouched; full suite 879/879. Engine selection per configuration, the `jsonmap` engine and the no-template path follow with the formatters. |
| 2026-09-19 | P1-03 envelope routing (slice 4) | done (slice 4) | `wp/p1-03-envelope-routing-4` | `Credential.signQRData` builds the claim-169 CWT through `CwtEnvelope` (`CoseHeaderPolicy.cwt()`: kid protected, no chain, tags 18/61; claims iss/exp/nbf/iat/169 with claim 169 as the PixelPass CBOR byte string; validity from typed `certify.signing.cwt.*`, defaults = keymanager's 180/0 days). Guarded by the claim-169 golden (JCA against the JWKS key) and a JCA-verified unit test; ArchUnit kernel store down 10; full suite 879/879. With this, every credential and token Certify signs goes through `certify-signing`; keymanager DTOs remain only in `SystemInfoController` (administration) and the dead `Credential.addProof` default. |
| 2026-09-19 | P0-03 Signature vectors (claim-169 CWT) | done (slice 4) | `wp/p0-03-vectors-cwt` | Golden for the QR path the compose `FarmerCredential` uses: an `ldp_vc` configuration with `qrSettings` + `qrSignatureAlgo=EdDSA`; the template embeds `claim_169_values`; the test decodes the PixelPass string, checks CWT tag 61 around COSE_Sign1 tag 18, protected `[alg, kid]`, claims `[iss, exp, nbf, iat, 169]` with claim 169 as `bstr .cbor` of the mapped map, and verifies the Ed25519 signature with JCA against the `jwks.json` key named by `kid`. Also fixed `certify-service/pom.xml`: the explicit `kotlin-stdlib:1.8.20` pin made every QR issuance fail with `NoClassDefFoundError: kotlin/text/HexExtensionsKt` (see findings). Stable over two replays; full suite 878/878. Every keymanager signing path now has a golden with independent verification. |
| 2026-09-18 | P1-14 certify-cli (first cut) | done (first cut) | `wp/p1-14-cli` | picocli module and shaded jar: `keys generate/list/jwks` over a PKCS#12 file (`jca` provider), `sign jws` (compact or RFC 7797 detached, typ/kid/x5c/x5t options, PS256 over RSA) and `sign cose` (COSE_Sign1 IssuerAuth layout, optional tag 18 and kid). In-process tests verify every output with Nimbus or JCA. Not yet: LD proofs, issue/batch/verify, keymanager provider. Jar is 53 MB (JSON-LD stack inside certify-signing) — split later. |
| 2026-09-18 | P1-04 KeyPublisher | done | `wp/p1-04-key-publisher` | `jwks.json` and `did.json` are built from `KeyProvider` descriptors (`KeyPublisher` bean; new `KeyProvider.publicKeys(KeyRef)` for every key an alias has had). Goldens for both documents recorded first (key material masked) and unchanged after the switch; JWKS field set per key identical to before; Ed25519 DID method no longer needs a BouncyCastle key class. `JwksServiceImplTest`/`DIDDocumentUtilTest` rewritten against fake providers; ArchUnit kernel store down 31; full suite 877/877. Remaining `io.mosip.kernel` users in `certify-service`: `SystemInfoController` (keymanager administration), `Credential.signQRData` (CWT), `Credential.addProof` (`vc`). |
| 2026-09-18 | P1-03 envelope routing (slice 3) | done (slice 3) | `wp/p1-03-envelope-routing-3` | `MDocProcessor.signMSO` builds the ISO 18013-5 IssuerAuth through `CoseEnvelope.sign1` with `CoseHeaderPolicy.mdocIssuerAuth()` (same layout keymanager produced: protected `alg`, unprotected `x5chain`, untagged); `CoseSignatureService` gone from the processor. Guarded by the mDoc golden (JCA verification against the x5chain leaf) and JCA-verified unit tests replacing the keymanager stubs; ArchUnit kernel store down 10; full suite 880/880. Left on keymanager DTOs: claim-169 `cwtSign` in `Credential.signQRData`, `Credential.addProof` (`vc`), `JwksServiceImpl`/`DIDDocumentUtil` (P1-04), `SystemInfoController`. |
| 2026-09-18 | P0-03 Signature vectors (mDoc) | done (slice 3) | `wp/p0-03-vectors-mdoc` | `mso_mdoc` golden: an mDL configuration (`docType`, `msoMdocClaims`, ES256 with `CERTIFY_VC_SIGN_EC_R1`) issued with a `did:jwk` holder proof; the IssuerAuth COSE_Sign1 is verified with plain JCA against the `x5chain` leaf (which is also a JWKS key), the MSO decoded with the CBOR library and checked structurally (docType, digests, validityInfo, device key = holder key); goldens for the response shape and an MSO summary (protected `alg -7`, unprotected `[33]`, untagged). Note for P1-03: this layout equals `CoseHeaderPolicy.mdocIssuerAuth()`. Stable over two replays; full suite 879/879. Still unguarded: claim-169 `cwtSign`, `Credential.addProof` (`vc`). |
| 2026-09-18 | P1-03 envelope routing (slice 2) | done (slice 2) | `wp/p1-03-envelope-routing-2` | `Ed25519Signature2018`, `EcdsaSecp256k1Signature2019` and `EcdsaKoblitzSignature2016` generators now produce their detached JWS through `LdLegacyEnvelope.detachedJws`; `AccessTokenJwtUtil` signs through `JwsEnvelope` (header `alg`, `kid` as before). Guarded by the slice-2 goldens (danubetech verification of the 2018/k1 proofs, Nimbus verification of the access token) and JCA-verified unit tests replacing the mock-passthrough ones; ArchUnit kernel store down another 55 references; full service suite 878/878. Remaining on keymanager DTOs: mDoc `coseSign1`, claim-169 `cwtSign`, `Credential.addProof`, `JwksServiceImpl`/`DIDDocumentUtil` (P1-04), `SystemInfoController`. |
| 2026-09-18 | P0-03 Signature vectors (second slice) | done (slice 2) | `wp/p0-03-vectors-slice-2` | Goldens now also lock `Ed25519Signature2018` (danubetech `Ed25519Signature2018LdVerifier`), `EcdsaSecp256k1Signature2019` (danubetech verifier over a JCA `ByteVerifier`), `EcdsaSecp256r1Signature2019` (danubetech canonicalization + JCA, because no third-party verifier exists for this suite; see findings) and the pre-authorized code flow (`/pre-authorized-data` → `/credential-offer-data/{id}` → `/oauth/token`, access token verified with Nimbus against `jwks.json`; goldens for the offer, the token response and the token claims and header). Metadata golden re-recorded because three configurations were added (fixture change, not behaviour). Still unguarded: mDoc `coseSign1`, claim-169 `cwtSign`, `Credential.addProof` (`vc`). Stable over two replays. |
| 2026-09-18 | P1-03 envelope routing (slice 1) | done (slice 1) | `wp/p1-03-envelope-routing` | The golden-guarded signing paths now sign through `KeyProviderRegistry` + `certify-signing`: `Ed25519Signature2020` and `EcdsaSecp256r1Signature2019` generators (`LdLegacyEnvelope.proofValueBase58`), `RSASignature2018` (`LdLegacyEnvelope.detachedJws`, RFC 7797 with `kid`/`x5t#S256`), the Data Integrity branch of `W3CJsonLD` (`SignerByteSigner`), `SDJWT.addProof` (`JwsEnvelope` with `JwsHeaderPolicy.sdJwtVc()`). `LegacyKeyRefs` maps `key_manager_app_id`/`ref_id` to `keymanager:APPID/REFID`; `KeymanagerByteSigner` deleted. `JwsEnvelope` fixed to assemble unencoded binary payloads as bytes. Mock-passthrough unit tests replaced by JCA/Nimbus-verified ones. Goldens unchanged (danubetech and Nimbus verification green); ArchUnit kernel store down another 57 references; full service suite 881 tests, 0 failures, 1 skipped. Slice 2 (ES256K legacy suites, mDoc COSE, claim-169 CWT, access tokens, `Credential.addProof`) waits for vectors; see the WP spec. |
| 2026-09-18 | P1-02 keymanager KeyProvider | done | `wp/p1-02-keymanager-provider` | New module `certify-keyprovider-keymanager`: `KeymanagerKeyProvider` (resolve by latest valid certificate or pinned key id, `signRaw` via `signv2` base64url = JOSE raw signature, `publicKeys` from the key-alias-mapper aliases, `ensureKeys` reproducing `initKeys`), `KeymanagerAlias` grammar `APPID/REFID`, `KeymanagerKeyInitializer` (master keys always; signing keys in DataProvider mode: the historical four plus mapper extras), `KeymanagerKeyProviderConfiguration` (a Boot auto-configuration) owning the kernel component scan and JPA wiring. `CertifyServiceApplication` no longer scans kernel packages; `AppConfig` lost its keymanager code. Lesson kept in the WP spec: module wiring goes through auto-configuration, not `@Import` on the application class, or `@WebMvcTest`/`@DataJpaTest` slices break. `SigningKey.withAlgorithm` for PS256 over RSA. 16 module tests against a keymanager stand-in (Nimbus-verified for five algorithms) and a wiring test booting the real service (H2 + PKCS#12) proving the provisioned keys sign verifiably and publish the same kids as the JWKS endpoint. Goldens unchanged; ArchUnit kernel store down 39 references; full service suite 893/893. Signing call sites still use keymanager directly until P1-03. |
| 2026-09-18 | P1-01 certify-spi + issuance core | done | `wp/p1-01-spi-and-issuance-core` | New modules `certify-spi` (27 types: configuration model, `IssuanceContext`, `HolderBinding`, `ClaimSet`, `Unsigned`/`IssuedCredential`, `TenantContext`, and the `CredentialFormatter`, `CredentialDataSource`, `ExternalIssuer`, `ProofValidator`, `StatusProvider`, `TemplateEngine`, `IssuanceListener` interfaces) and `certify-issuance` (`IssuanceCommand` with draft-13 selector, sealed `IssuanceResult`, `DefaultIssuanceService` running the eight-step flow, `FormatterRegistry`, `KeyProviderRegistry`, `AuthorizationPolicy.SCOPE`, `InMemoryConfigurationRegistry`). 23 component tests with fakes and the real `JcaKeyProvider`; the issued JWS verifies with Nimbus; ArchUnit keeps spi/issuance/signing free of Spring, JPA, Velocity, keymanager and `certify-core`. Not yet wired into `certify-service`. Named `certify-issuance` because the `certify-core` artifact is the existing DTO module (decision logged). |
| 2026-09-18 | P0-03 Signature vectors (first slice) | partial | `wp/p0-03-signature-vectors` | Signing paths now locked by end-to-end goldens with independent verification: `Ed25519Signature2020` (legacy multibase, danubetech), `eddsa-rdfc-2022` (Data Integrity over VC 2.0, danubetech `DataIntegrityProofLdVerifier`), `RsaSignature2018` (detached JWS, danubetech), SD-JWT `jwsSignV2` ES256 (Nimbus against `jwks.json`). Still unguarded: `EcdsaSecp256r1Signature2019`, ES256K suites, mDoc `coseSign1`, claim-169 `cwtSign`, access-token `jwsSign`; to add before P1-03 touches those paths. |
| 2026-09-18 | P0-01 Golden tests (v1) | done (first slice) | `wp/p0-01-golden-tests-v1` | `IssuanceGoldenTest` boots the real service in the test JVM (H2, PKCS#12 keymanager, Velocity, keymanager signing; profiles `local,test`), creates an `ldp_vc` (Ed25519Signature2020) and a `dc+sd-jwt` (ES256) configuration, fetches a nonce, builds a holder proof and issues through `POST /issuance/credential`; the ldp_vc verifies with danubetech against `did.json`, the SD-JWT with Nimbus against `jwks.json`; normalized goldens under `goldens/v1` (metadata, both credentials, two error bodies), stable over three runs. Fixed the stale H2 `credential_config` schema (missing `signature_crypto_suite`, `credential_status_purpose`, `qr_*`, claim columns; wrong PK). Remaining paths (Data Integrity, RSA, ES256K, mDoc, claim-169 CWT, access tokens) follow as P0-03. |
| 2026-09-18 | P0-08 CredentialRegistry | done | `wp/p0-08-credential-registry` | `CredentialRegistry` (core SPI) with `CredentialRegistryImpl` caching the issuer-metadata view in `issuerMetadataCache` (programmatic `CacheManager`, evicted by `@CacheEvict` on every config write); `CertifyIssuanceServiceImpl`, `VCIssuanceServiceImpl`, `PreAuthorizedCodeService` and `WellKnownController` read through it, so a credential request no longer runs `findAll()`; `VCIssuanceUtil.toCredentialConfigurationSupported` extracted. |
| 2026-09-18 | P0-07 AuthorizationContext | done | `wp/p0-07-authorization-context` | `AuthorizationContext` (plain, tenant-aware, in `certify-core`) as a request-scoped bean; `ParsedAccessToken` is now a stateless delegate bean (`RequestScopedParsedAccessToken`, `@Primary`) so filters, services and plugins are thread-safe without code changes and fall back to local fields outside a request; `ParsedAccessToken` deprecated for plugins. Two-thread isolation test. Also fixed a regression from P0-09: `@WebMvcTest` slices have no `MeterRegistry`, so the interceptor now resolves it lazily. Full suite: 877 tests, 0 failures. |
| 2026-09-18 | P0-04 Flyway baseline + P0-05 Testcontainers | done | `wp/p0-04-flyway-baseline` | `flyway-core` with per-module locations `db/migration/{core,keymanager,verify,as}` and baselines `V1_0_0_000..003` generated from `db_scripts` by `db_scripts/generate_flyway_baseline.sh`; `FlywayDefaults` (baseline-on-migrate at `1.0.0.003`, schema from `currentSchema`, locations) applies unless `spring.flyway.*` is set; `io.mosip.certify.tools.MigrateOnly` entry point (`-Dloader.main=...`) and a Helm pre-install/pre-upgrade Job (`dbMigration.enabled`); H2 test profiles disable Flyway. `FlywayMigrationTest` on Testcontainers PostgreSQL 15: empty DB gets 15 tables from the baselines, a `db_scripts` DB is baselined with zero migrations, and both roads produce identical columns, indexes and constraints. Needed Testcontainers 1.21.4 (Docker Engine 29 removed API < 1.44) and docker-java 3.5.3. |
| 2026-09-18 | P0-10 Signing skeleton | done | `wp/p0-10-signing-skeleton` | New modules `certify-signing` (`SignatureAlgorithm`, `AlgorithmRegistry` with the JOSE/COSE/LD-suite/cryptosuite table, `KeyRef`, `CertificateChain`, `PublicKeyDescriptor` with JWK export, `SigningKey`, `KeyProvider`, `Signer`, `KidStrategy` PROVIDER/JWK_THUMBPRINT/X5T_S256, `JwsHeaderPolicy`, `CoseHeaderPolicy`, `JwsEnvelope`, `CoseEnvelope`, `CwtEnvelope`, `KeyPublisher`) and `certify-keyprovider-jca` (PKCS#12 load/save, dev mode generating ES256/EdDSA/RS256/PS256/ES256K keys with self-signed certificates, `ensureKeys`). 21 tests: every JWS verified by Nimbus, every COSE_Sign1 by plain JCA. Data Integrity and legacy LD envelope builders arrive with P1-03 (they need the danubetech stack). Not yet wired into `certify-service`. Also fixed the P0-09 interceptor constructor ambiguity that broke context boot. |
| 2026-09-18 | P0-09 Deprecation infrastructure | done | `wp/p0-09-deprecation-infra` | `@DeprecatedEndpoint` + `DeprecationInterceptor`: RFC 9745 `Deprecation`, RFC 8594 `Sunset`, `Link rel=deprecation/successor-version`, Micrometer `certify.deprecated.calls{endpoint,enabled}`, hourly log throttle, kill switch `mosip.certify.deprecated.<name>.enabled=false` answering 410 JSON; OpenAPI `deprecated` flag when springdoc is present. No endpoint annotated yet. |
| 2026-09-18 | P0-06 ArchUnit rules | done | `wp/p0-06-archunit-rules` | Six frozen rules; store holds 246 `io.mosip.kernel` references outside a provider module, 5 Spring Web uses in `certify-core`, 4 repository reads in the template engine; controllers and VCFormats rules currently clean (VCFormats constants are inlined by javac, so that rule bites only after P1) |
| 2026-09-18 | Baseline | done | `design/extensibility` | Existing suite on JDK 21: 858 tests, 0 failures, 1 skipped, BUILD SUCCESS. Build needs `JAVA_HOME` on JDK 21, `-Dgpg.skip=true`, and locally `.mvn/settings-local.xml` because JitPack hangs on missing coordinates; see CLAUDE.md |

## Findings about the existing test suite ("trust but verify")

- QR (claim-169) issuance was broken on `develop` as built from this pom: `certify-service/pom.xml` pinned `kotlin-stdlib` to 1.8.20 while `pixelpass-jar` 0.8.0 (and every other Kotlin artifact via Spring Boot) needs 1.9.22, so `PixelPass.generateQRData` threw `NoClassDefFoundError: kotlin/text/HexExtensionsKt` and any configuration with `qr_settings` (the compose FarmerCredential) answered 500. The existing unit tests mock PixelPass and keymanager and never reached it; the new golden did. Fixed by letting Spring Boot manage the version.
- The H2 `schema.sql` for `credential_config` had drifted from the DDL (no `signature_crypto_suite`, `credential_status_purpose`, `qr_settings`, `qr_signature_algo`, `mso_mdoc_claims`, `sd_jwt_claims`; `vct` instead of `sd_jwt_vct`; composite PK) because no test ever inserted a configuration; fixed in P0-01.
- The test profile's signing properties are placeholders (`TestSignature`, `TEST_APP_ID`), so nothing in the old suite exercised a real signing path end to end.

## Spec and behaviour findings from the goldens (to fix in the adapters, not silently)

- The claim-169 CWT carries `kid` but no `x5chain`: `Credential.signQRData` asks keymanager with `protectedHeader {x5c: true, kid: true}` but keymanager's `CoseHeaderBuilder` only honours its own keys, so the certificate is never included and a verifier must resolve the key from `jwks.json` by `kid`. Keymanager also adds `exp`, `nbf` and `iat` with its own defaults. The `CwtEnvelope` route should include the chain and take the validity from configuration.
- `did.json` lists the DID itself in `assertionMethod` and `authentication` instead of verification-method ids (DID Core §5.3.1 expects references to verification methods); `alsoKnownAs` and `service` are emitted as empty arrays. Verifiers that resolve `verificationMethod` directly still work, which is why the credentials verify.
- `EcdsaSecp256r1Signature2019` (P-256, `proofValue` base58btc) is Certify's own suite name: it is not in the W3C LD cryptosuite registry and no library outside Certify verifies it (danubetech has k1 and Ed25519 only). Spec-correct P-256 issuance is `ecdsa-rdfc-2019` (Data Integrity), which Certify already supports; the legacy suite should be deprecated on the compatibility surface.
- `POST /pre-authorized-data` answers HTTP 200 with `{"errors":[{"errorCode":"unknown_claims"}]}` (legacy `ResponseWrapper` envelope) instead of an OAuth-style 4xx; the new `/oid4vci` surface must not inherit this.
- The credential offer's `credential_issuer` is `mosip.certify.identifier` (with servlet path) while the issuer metadata's `credential_issuer` is `mosip.certify.domain.url`; OpenID4VCI requires the same Credential Issuer Identifier in both (second face of the dual-identity finding above).
- The access token's `sub` is the offer's claims map serialized as JSON in `HashMap` order (PII inside the token, nondeterministic), `client_id` is the empty string, and `aud` is built by string concatenation (`${mosip.certify.identifier}/v1/certify/issuance/credential`), which duplicates the servlet path whenever the identifier already carries it (as in the test profile).
- Issuer metadata orders `credential_metadata.claims` by `HashMap` iteration, so the document differs between JVM runs; the order should follow the configured display order.
- Optional display fields absent from a configuration are serialized as `null` (`logo`, `background_color`, `background_image`, `text_color`, `claims[].display`); OpenID4VCI defines them as optional objects and strings, so they should be omitted (`@JsonInclude(NON_NULL)` on `MetaDataDisplayDTO` and `CredentialMetadataDTO.Claims`).
- `mosip.certify.identifier` (proof audience, SD-JWT `iss`) and `mosip.certify.domain.url` (`credential_issuer` in metadata) are two keys for one identity; in the test profile they already disagree (`.../v1/certify` vs `http://localhost:8090`). See docs/design/14-configuration.md.
- `credential_definition.type` is emitted sorted alphabetically (`["GoldenCredential","VerifiableCredential"]`) because the mapper stores types as a sorted comma list; the issuer's configured order is lost.
- On H2, `TEXT[]` columns come back as one stringified element (`"[did:jwk, did:web]"`); PostgreSQL returns a proper array. One more reason the repository tests must run on PostgreSQL (P0-05).

- `CertifyApplicationTests.test()` boots the application a second time by calling `CertifyServiceApplication.main()` inside a `@SpringBootTest`; it asserts only that the class is not null.
- Controller tests are `@WebMvcTest` with mocked services; no test exercises an end-to-end issuance through the real signing path.
- Repository tests run on H2 with `schema.sql`, not on the PostgreSQL DDL under `db_scripts` (JSONB, `TEXT[]`, enum and partial indexes are never exercised).
