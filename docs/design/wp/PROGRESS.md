# Rebuild progress log

Newest first. One entry per work package or notable finding. Branch names are on `jainhitesh9998/inji-certify`.

| When (UTC) | WP | State | Branch | Notes |
| --- | --- | --- | --- | --- |
| 2026-09-19 | R-05 legacy credential classes without the kernel signing path | done | `wp/r-05-credential-classes` | Charter area 3, smallest files first: the dead base `addProof` through keymanager's `SignatureService`, the shadowed `SignatureServicev2` field in `W3CJsonLD`, the duplicated `KeyProviderRegistry` fields and the unused `headers` parameter are gone; `credential/*` imports nothing from `io.mosip.kernel`. No behaviour change. Full service suite 1034 green. |
| 2026-09-19 | R-04 issuer metadata building out of the configuration service | done | `wp/r-04-metadata-builder` | Charter area 2: `CredentialIssuerMetadataBuilder` builds the compatibility metadata document from the active rows; `CredentialConfigurationServiceImpl` keeps CRUD, validation, templates and `protocolDefaults` (509 lines to 345). No behaviour change. Full service suite 1035 green. |
| 2026-09-19 | R-03 one `CredentialUtils`, helpers next to their only caller | done | `wp/r-03-credential-utils` | First slice of the file-by-file pass (charter R-00, area 1): the second `CredentialUtils` class is gone, the dead `isVC2_0Request` deleted, the LD proof builders live in `W3CJsonLD`, `toJsonMap` in the legacy issuance service, and `validateAndGetClientNonce` no longer takes a logger. No behaviour change. Full service suite 1035 green. |
| 2026-09-19 | P3-11 `did:web` holder keys behind a switch, `did:key` in the advertised default | done | `wp/p3-11-did-web-holders` | Owner's decision on the V-01 finding: `DIDwebProofManager` resolves `did:web` kids over HTTPS when `certify.protocol.oid4vci-v1.did-web-holders.enabled` is set (off by default, both surfaces unchanged otherwise); the local profile advertises `did:jwk` and `did:key`, as the compose profile already did. Full service suite 1036 green. |
| 2026-09-19 | P3-10 key attestations (`key_attestation` header, `attestation` proof type) | done | `wp/p3-10-key-attestation` | OpenID4VCI 1.0 Appendix D and F on the new surface: `KeyAttestationValidator` trusts attesters under `certify.protocol.oid4vci-v1.key-attestation.attesters.<id>` (a JWK Set or a PEM trust anchor for `x5c`); a configuration requires one through `proof_types_supported.jwt.key_attestations_required`; the `attestation` proof type issues one credential per attested key, `batch_size` capping the total. `Oid4vciV1Properties` renamed `Oid4vciProperties` on the owner's remark. Conformance gap item 6. Full service suite 1030 green. |
| 2026-09-19 | V-01 holder key forms and issuer DID methods validated | done | `wp/v-01-holder-did-methods` | `HolderDidMethodsTest` issues on both surfaces with every holder key form 1.0.0-beta.1 accepted (`jwk` header P-256/Ed25519/RSA, `kid` as `did:jwk`, `did:key` Ed25519/P-256/secp256k1/RSA). Finding: RSA `did:key` never verified on develop (JWK built without `kid`), fixed. `did:web` holders are advertised by the local profile but not resolvable, on develop too; decision asked. Record: `docs/design/18-compatibility-validation.md`. Full service suite 1015 green. |
| 2026-09-19 | P3-09 Token Status List for SD-JWT VC and mDoc | done | `wp/p3-09-token-status-list` | `status.status_list = {idx, uri}` on SD-JWT VC and mDoc (`status_config.mechanism = TokenStatusList`), lists as `statuslist+jwt` rows of the existing status tables signed under `certify.status.token-status-list.key-ref`, served at `/credentials/token-status-list/{id}`, re-signed by the batch job. Conformance gap item 5. Also fixes `DIDDocumentUtil.fetchKeys` (a configuration signed by a non-keymanager provider no longer breaks did.json, the cause of the red integration run at a098f2e) and the v2 mapper defaults for formats without deployment defaults. Full service suite 995 green. |
| 2026-09-19 | P3-08 X.509 chains for SD-JWT VC and mDoc | done | `wp/p3-08-pki-chains` | x509-file dev mode signs generated keys with a dev CA; `ChainInclusion.WITHOUT_ANCHOR`; `signing_config.x5c` per configuration (v2 API `signing.x5c`); `CertificateChainPolicy` before SD-JWT and mDoc signing; the service now ships the x509-file module (it never depended on it). Conformance gap item 4. Full service suite 993 green. |
| 2026-09-19 | P3-07 DPoP-bound access tokens | done | `wp/p3-07-dpop-tokens` | A `DPoP` proof at `POST /oauth/token` (RFC 9449 section 5) binds the token (`cnf.jkt`, `token_type: DPoP`) on every grant; `dpop_signing_alg_values_supported` advertised; Bearer without a proof. Conformance gap item 3. Full service suite 992 green. |
| 2026-09-19 | R-02 application boot test | done | `wp/p3-06-client-attestation` | `CertifyApplicationTests` booted a second application through `main()` inside the test context (port 8090, a shared keystore that corrupted a P-256 key encoding for the golden tests once). It is a context-loads test now. |
| 2026-09-19 | P3-06 attestation-based client authentication | done | `wp/p3-06-client-attestation` | `OAuth-Client-Attestation` and `-PoP` validated at `POST /oauth/par` and `POST /oauth/token` against attesters under `certify.as.client-attestation.attesters.<id>.jwks`; `required=true` refuses unauthenticated calls (HAIP); `token_endpoint_auth_methods_supported` advertised. Conformance gap item 2. Full service suite 991 green. |
| 2026-09-19 | P3-05 authorization code flow in Certify's own AS | done | `wp/p3-05-authorization-code` | `POST /oauth/par` and `GET /oauth/authorize` (PKCE S256, scope or authorization_details, redirect with code, state and iss) for clients registered under `certify.as.clients`; subject from `certify.as.authorization.subject-mode` (`fixed` for conformance); the existing token endpoint exchanges the code. Conformance gap item 1. Full service suite 990 green. |
| 2026-09-19 | R-01 golden set names | done | `wp/r-01-golden-set-names` | Owner: `goldens/v1`, `d13`, `v2` were not proper names. Renamed to `legacy-develop` (today's endpoints as develop answers), `legacy-0.14.0` (the draft-13 surface of release 0.14.0) and `oid4vci-1.0` (the new surface); every reference in tests and docs rewritten; `goldens/README.md` explains the sets. Files unchanged, only moved. Full service suite 988 green. |
| 2026-09-19 | R-00 refactor charter | done | `design/extensibility` | Owner-requested file-by-file refactor of the legacy service code; rules and inventory in `R-00-refactor-charter.md`; slices follow as `R-nn`. |
| 2026-09-19 | P3-04 subject in the pre-authorized offer | done | `wp/p3-04-preauth-subject` | `POST /pre-authorized-data` takes an optional `subject` that becomes the access token `sub`, so the CSV plugin (and any `sub`-resolving plugin) works in the pre-authorized code flow without eSignet; claims-based offers unchanged. Owner's request of 2026-09-19. Compose smoke run with the CSV plugin: 20 checks green, all 15 template fields resolved from the CSV row. Full service suite 988 green. |
| 2026-09-19 | P2-11 per-tenant issuer display and authorization servers | done | `wp/p2-11-tenant-issuer-display` | `certify.tenancy.tenants.<id>.display` and `.authorization-servers` replace the deployment's values in the tenant's `/oid4vci` metadata document; the default document is untouched. Full service suite 983 green. |
| 2026-09-19 | P2-10 path tenant resolver | done | `wp/p2-10-path-tenant-resolver` | `certify.tenancy.resolver=path`: a configured tenant lives under `/t/{tenant}/oid4vci/...` with `{deployment}/t/{tenant}/oid4vci` as its identifier; new-surface controllers mapped at both forms; unknown path tenants are the default. Full service suite 983 green. |
| 2026-09-19 | P2-09 upgrade rehearsal from a 0.14.0 database | done | `wp/p2-09-upgrade-from-0.14.0` | Phase 2 exit criterion: the vendored 0.14.0 DDL with 0.14.0-shaped rows, the shipped `0.14.0_to_1.0.0` script, Flyway baseline plus 1.1.0 with backfill, the 1.1.0 rollback and re-upgrade, compared column by column with a fresh Flyway schema (`UpgradeFrom014Test`): no difference. Full service suite 980 green. |
| 2026-09-19 | P2-08 mixed-strategy deployment test | done | `wp/p2-08-mixed-strategy` | Phase 2 exit criterion: one deployment (DataProvider mode) issues a templated configuration and an `EXTERNAL` one (`dataSourceId: vci-plugin`, written through the v2 API) through `/oid4vci/credential`; no production change needed. Full service suite 979 green. |
| 2026-09-19 | P1-11f deprecation headers on the compatibility credential endpoint | done | `wp/p1-11f-compat-credential-deprecation` | `POST /issuance/credential` (develop body) carries `@DeprecatedEndpoint(oid4vci-v1-compat-credential)`: `Deprecation`/`Link` headers, `certify.deprecated.calls` counter, kill switch `mosip.certify.deprecated.oid4vci-v1-compat-credential.enabled=false` (410), OpenAPI flag, release note; bodies and goldens unchanged. Merged on the owner's instruction of 2026-09-19; reminder: the remaining compatibility endpoints (the legacy `.well-known` documents, `/nonce` stays) get their headers with the compat-core flip. |
| 2026-09-19 | P1-11e issuer identifier with the servlet path | done | `wp/p1-11e-issuer-servlet-path` | Smoke-run finding: with `mosip.certify.identifier` set to the bare domain (the compose stack), the new surface advertised endpoints without `/v1/certify`. `Oid4vciIssuer.derive` now appends `server.servlet.path` unless the identifier already ends with it; goldens unchanged. Also from the smoke run: plugin jars path, `mosip_network`, and `PreAuthDataProviderPlugin` in the `rebuild` profile. Full service suite 978 green (`CertifyApplicationTests` needs port 8090 free). |
| 2026-09-19 | P2-07 tenant DID documents and verification methods | done | `wp/p2-07-tenant-did-document` | The core signs a tenant request with the tenant's DID as issuer (`proof.verificationMethod` = `<tenant did>#<kid>`); `GET /.well-known/did.json` on a tenant host publishes the deployment's keys under the tenant's DID; the acme credential verifies with danubetech from the tenant's document. Default tenant and goldens untouched. Full service suite 975 green. |
| 2026-09-19 | P3-03 single-use nonces and batch issuance on the new surface | done | `wp/p3-03-single-use-nonce-batch` | A `c_nonce` from `POST /oid4vci/nonce` is consumed by the credential request it authorised (`certify.protocol.oid4vci-v1.nonce.single-use`, on); `proofs` above `certify.protocol.oid4vci-v1.batch.size` (10) answer `invalid_credential_request`; `batch_credential_issuance.batch_size` advertised on the new surface. Compatibility surfaces unchanged. Full service suite 975 green. |
| 2026-09-19 | P2-06 v2 configuration API with preview | done | `wp/p2-06-config-api-v2` | `/v2/credential-configurations` (POST, GET, PUT, DELETE, `POST /{id}/preview`) writes the JSONB model with every legacy column mirrored, versions templates in `credential_template`, dry-runs a body with `sampleClaims` through the core (`IssuanceService.preview`) inside the write transaction, answers `{error, error_description}` with HTTP statuses; tenant of the request. Full service suite 971 green. |
| 2026-09-19 | P3-02 issuance transactions and the notification endpoint | done | `wp/p3-02-issuance-transactions` | First writer of the 1.1.0 `issuance_transaction` table (`IssuanceTransactionListener`, one `ISSUED` row per core issuance, purged after `certify.protocol.oid4vci-v1.notification.retention`); `notification_id` in the `/oid4vci/credential` response; `POST /oid4vci/notification` (204, `invalid_notification_id`, `invalid_notification_request`); `notification_endpoint` in the default and per-tenant metadata. Three v2 goldens re-recorded for the new field; v1 and d13 untouched. Full service suite 969 green. |
| 2026-09-19 | P2-05 per-tenant issuer metadata on the new surface | done | `wp/p2-05-tenant-metadata` | `GET /oid4vci/.well-known/openid-credential-issuer` for a resolved tenant lists that tenant's configurations under its own identifier and endpoints (`TenantIssuerMetadata` from formatter fragments and the configuration model); the default tenant keeps develop's document; the legacy and draft-13 document builders keep only the default tenant's rows. Full service suite 968 green. |
| 2026-09-19 | P1-11d audit parity on the core path | done | `wp/p1-11d-audit-parity` | `AuditListener` writes `PROOF_VALIDATION` success and error (proof and nonce failures) next to `VC_ISSUANCE`, as the legacy services did; `NONCE_VALIDATION` still needs a per-proof hook. Full service suite 967 green. |
| 2026-09-19 | P2-04 credential_template read by the registry | done | `wp/p2-04-credential-template` | `CredentialTemplate` entity and repository; the registry reads the template a configuration names by `template_id`/`template_version` (decoded text) and falls back to the `vc_template` blob; the v1 config API mirrors every template as version 1 on add and update. Every golden renders from the table. Full service suite 965 green. |
| 2026-09-19 | P2-03 TenantResolver | done | `wp/p2-03-tenant-resolver` | `certify.tenancy.*` typed settings, `TenantResolver.fixed(default)` unless enabled with the `host` resolver, `TenantResolutionFilter` into the request-scoped `AuthorizationContext`, `TenantContexts` giving each adapter the tenant's issuer identifier and DID, tenant-scoped registry lookups. `TenancyIssuanceTest` issues for an `acme` host with acme's DID and audience while the default tenant is unchanged. Full service suite 964 green. |
| 2026-09-19 | P2-02 v2 configuration model: read path and dual write | done | `wp/p2-02-config-v2-read-path` | `CredentialConfig` carries the 1.1.0 columns; `ConfigV2Columns.fill` derives them exactly as the migration backfills them and the v1 config API calls it on every save; `JpaConfigurationRegistry` reads the JSONB model when `config_version >= 2` (strategy column honoured for EXTERNAL/SUPPLIED, template still from `vc_template`). The two read paths are compared row by row in the registry test, and every golden test now runs on v2 rows. Full service suite 960 green. |
| 2026-09-19 | P2-01 configuration model v2 and tenant columns (1.1.0 migration) | done | `wp/p2-01-config-v2-migration` | `V1_1_0_000__config_v2_and_tenancy.sql`: JSONB model beside the legacy columns with backfill, `credential_template` decoded from the base64 blob, `issuance_transaction`, ledger columns, `tenant_id` everywhere tenant-scoped, unique indexes rebuilt to lead with `tenant_id`; rollback script under `db_upgrade_script`. `FlywayMigrationTest` now expects five migrations and 17 tables; `ConfigV2MigrationTest` verifies the backfill row by row and the rollback on PostgreSQL. No reader of the new columns yet (P2-02). Full service suite 958 green. |
| 2026-09-19 | P1-15 legacy plugin adapters | done | `wp/p1-15-legacy-plugin-adapters` | `LegacyExternalIssuer` brings `VCIssuancePlugin` into the core with the legacy calls and error codes; the compat-core flag now applies to both plugin modes. `VcIssuancePluginGoldenTest` records the VCIssuance-mode answers of the legacy service (`goldens/legacy-develop/vci-plugin`) and `VcIssuancePluginGoldenCoreTest` replays them through the core, identical. Full service suite 957 green. |
| 2026-09-19 | P1-11c compatibility path through the new core | done | `wp/p1-11c-compat-core` | `certify.protocol.oid4vci-v1.compat-core.enabled` routes `POST /issuance/credential` (1.0 body) through `DefaultIssuanceService` (`CoreBackedVCIssuanceService`, DataProvider mode): SPI `beforeRender` hook; legacy template parameters, render-method digest, claim-169 QR and audit as listeners. `IssuanceGoldenCoreTest` and `StatusListPostgresCoreTest` replay every v1 golden and the PostgreSQL status flow through the core unchanged. Flag off by default until CI has run both modes; then the default flips and `CertifyIssuanceServiceImpl` goes. Full service suite 946 green. |
| 2026-09-19 | P1-12 oid4vci-d13 adapter (second slice: discovery) | done | `wp/p1-12-oid4vci-d13-metadata` | `?version=latest|vd12|vd11` on `/.well-known/openid-credential-issuer` (the unversioned document unchanged), the `/issuance/.well-known/openid-credential-issuer` and `did.json` aliases, all deprecated from day one; 0.14.0 shapes rebuilt from the same rows (`D13MetadataService`). The five metadata goldens replay; `Goldens` folds the nested-bracket H2 artefact 0.14.0 produced. Full service suite 917 green. |
| 2026-09-19 | P1-12 oid4vci-d13 adapter (first slice) | done (first slice) | `wp/p1-12-oid4vci-d13` | The 0.14.0 credential endpoints back through the new core: draft-13 body on `/issuance/credential` (dispatch on the body next to the 1.0 controller), `/issuance/vd12|vd11/credential`, 0.14.0 lookup and error answers, token-bound `c_nonce` with the fresh nonce inside `invalid_proof`, `vc+sd-jwt` typ, mDoc Document wrapper with tagged `issuerAuth`; deprecated from day one with headers, counter and kill switches. `D13GoldenReplayTest` reproduces every P0-02 issuance and error golden (mDoc keeps `signed` and tag 24 by decision). Full service suite 915 green. |
| 2026-09-19 | P0-02 draft-13 goldens from 0.14.0 | done | `wp/p0-02-golden-tests-d13` | 26 goldens under `goldens/legacy-0.14.0` recorded in process from a worktree of `e54539a` (`docs/design/wp/p0-02-recorder`): draft-13 credential requests for `ldp_vc`, `vc+sd-jwt`, `mso_mdoc`, vd11/vd12 paths, `invalid_proof` with `c_nonce`, versioned metadata, well-known documents, pre-authorized flow. `P0-02-notes.md` lists the differences against develop and what `oid4vci-d13` must restore. `D13GoldenSetTest` guards the set (replay disabled until P1-12). `Goldens.normalize` made idempotent (v1 `did.json`/`jwks.json` re-recorded, order only). Full service suite 904 green. |
| 2026-09-19 | P1-10a status list end to end on PostgreSQL | done | `wp/p1-10a-status-postgres` | `StatusListPostgresTest`: Testcontainers PostgreSQL prepared like a deployment (Flyway chain, then the `key_policy_def` rows of `db_scripts`), the service starts on it with Flyway enabled. VC 2.0 `ldp_vc` with a revocation purpose issued on both surfaces (same list, own index); credential and Bitstring Status List verified with danubetech; revocation via `/credentials/status` and the batch job flips the bit and the re-signed list verifies; ledger row checked. Three goldens recorded. Fix: generated status lists now carry the `ed25519-2020` suite context (their proof type expanded to an undefined term). Full service suite 902 green. |
| 2026-09-19 | P3-01 x509-file key provider | done | `wp/p3-01-x509-file-provider` | New module `certify-keyprovider-x509-file`: PKCS#12 file provider (`certify.keyprovider.x509-file.*`, off by default) with dev-mode generation of the listed keys and self-signed certificates; `JcaKeyProvider.fromPkcs12(id, ...)`; a `key_manager_app_id` written as `provider:alias` selects another provider on the new surface. Tests for dev/production behaviour and the registry parsing; full service suite 898 green. |
| 2026-09-19 | P1-10 status + listeners (first slice) | done (first slice) | `wp/p1-10-status-ledger` | `BitstringStatusProvider` over the status-list service (VC 2.0 `ldp_vc` with purposes), `CredentialIdListener` (legacy id prefix + UUID) and `LedgerListener` (ledger entry from the signed document and the plugin data kept in the claim set provenance) on the new surface; unit-tested with mocks; full suite 897/897. Finding: status lists need PostgreSQL (`generate_series`), so the H2 golden context cannot attach an entry end to end; Testcontainers golden is the follow-up. |
| 2026-09-19 | P1-11 oid4vci-v1 adapter (second slice: discovery) | done | `wp/p1-11-oid4vci-discovery` | The new surface has its own Credential Issuer Identifier (`mosip.certify.identifier` + `/oid4vci`, overridable), `GET /oid4vci/.well-known/openid-credential-issuer` (same configurations, this surface's endpoints) and `POST /oid4vci/nonce` (shared store); proofs are validated against the new identifier (`JwtProofValidator` audience-aware overload); SD-JWT `iss` is the new identifier. Goldens: metadata recorded, SD-JWT payload re-recorded. A wallet pointed at the new metadata can now complete the flow on the new surface (compose nginx maps `/oid4vci/`); full suite 894/894. |
| 2026-09-19 | P1-07 mDoc formatter | done | `wp/p1-07-mdoc-formatter` | New module `certify-format-mdoc`: the mDoc processor moved out of the service (no Spring/keymanager/certify-core types; typed `certify.format.mdoc.*`), `MdocFormatter` building salted namespaces, the MSO with the holder's device key and the IssuerAuth through `CoseEnvelope`; auto-configured and wired; `/oid4vci/credential` issues `mso_mdoc` (new-surface golden, IssuerAuth verified with JCA); full suite 893/893. All three formats of the design now issue on the new surface. |
| 2026-09-19 | P1-06 SD-JWT formatter | done | `wp/p1-06-sd-jwt-formatter` | New module `certify-format-sd-jwt`: `SdJwtFormatter` (payload with `vct`/`iss`/`cnf`, `_sd` digests from the configured JSON paths via the moved `SdJsonUtils`, issuer JWS with the sd-jwt-vc header policy, `~disclosures~`), auto-configured and wired; `/oid4vci/credential` issues SD-JWT (new-surface goldens, Nimbus-verified); full suite /. `_issuer` precedence fixed in both engines (DID, then `didUrl`, then identifier). Findings kept: `cnf.kid` instead of `cnf.jwk`, no `iat`/`exp`. |
| 2026-09-19 | P1-11 oid4vci-v1 adapter (first slice) | done (first slice) | `wp/p1-11-oid4vci-adapter` | `POST /oid4vci/credential` issues through `DefaultIssuanceService` end to end (registry over `credential_config`, Velocity engine, `DataProviderPlugin` data source, jwt proof adapter with the cache nonce check, `KeyProviderRegistry`, `ldp_vc` formatter); adapter-declared security (own `SecurityFilterChain` + token-filter subclasses per profile; no URL-list changes); OpenID4VCI error bodies with the core's spec names. New-surface golden `v2/oid4vci/ldp_vc-response` verified with danubetech. Legacy goldens unchanged; full suite 891/891. SD-JWT/mDoc formatters, metadata/nonce under `/oid4vci`, listeners and status follow. |
| 2026-09-19 | P1-05 ldp_vc formatter | done | `wp/p1-05-ldp-vc-formatter` | New module `certify-format-ldp-vc`: `LdpVcFormatter` adds Data Integrity proofs (danubetech `LdSigner` over `SignerByteSigner`) or legacy LD suite proofs (`LdLegacyEnvelope`, URDNA2015) through the core's `SigningContext`; auto-configured over the service's `StaticContextLoader`. Six tests verify every suite with danubetech verifiers against dev keys; goldens unchanged, full suite 889/889. The legacy `W3CJsonLD` path stays until P1-13. |
| 2026-09-19 | P1-11a ConfigurationRegistry | done | `wp/p1-11a-configuration-registry` | `JpaConfigurationRegistry` reads `credential_config` rows as SPI `CredentialConfiguration`s (selectors per format, velocity `TemplateRef`, `keymanager:APPID/REFID` signing, strategy from plugin mode, Bitstring status purposes, display); read-only, active rows, single tenant. Unit tests over a mocked repository and a real-context check in the golden suite; full suite 889/889. Bridge for the `/oid4vci` adapter (P1-11) and the CLI `issue` command. |
| 2026-09-19 | P1-08 TemplateEngine (second slice: jsonmap) | done | `wp/p1-08-jsonmap` | New module `certify-template-jsonmap`: the no-code engine (JSON document with `${claim}` placeholders, typed substitution, nested paths, fallbacks, issuance built-ins, template params, `$claims` spread; nothing executed), auto-configured. Five tests. With Velocity (first slice) and `TemplateRef.Mode.NONE` in the core, the three templating options of the design exist behind one SPI. |
| 2026-09-19 | P1-09 ProofValidator (first slice) | done (first slice) | `wp/p1-09-proof-validator` | `JwtProofValidatorAdapter` implements the SPI `ProofValidator` over today's `JwtProofValidator`: nonce via the adapter's `NonceCheck`, allowed algorithms and client id from `ProofPolicy`, holder returned as the derived `did:jwk`/`did:key` `HolderBinding`, header violations named (`proof_header_invalid_typ`/`_alg`) instead of the legacy boolean. Five tests with real ES256 proofs; full suite 884/884. Legacy path untouched; `DefaultIssuanceService` can now be fed real proofs once the adapter surface (P1-11) exists. |
| 2026-09-19 | P1-08 TemplateEngine (first slice) | done (first slice) | `wp/p1-08-template-engine` | New module `certify-template-velocity`: `VelocityRenderer` (the exact Velocity 1.7 + tools setup), `VelocityTemplateEngine` implementing the SPI `TemplateEngine` for the new core (claims, `_issuer`, `_holderId`, validity, params, tools; JSON object out), auto-configured. The legacy `VCFormatter` (`VelocityTemplatingEngineImpl`) now evaluates through the shared renderer instead of its own engine; goldens unchanged, its 22 tests untouched; full suite 879/879. Engine selection per configuration, the `jsonmap` engine and the no-template path follow with the formatters. |
| 2026-09-19 | P1-03 envelope routing (slice 4) | done (slice 4) | `wp/p1-03-envelope-routing-4` | `Credential.signQRData` builds the claim-169 CWT through `CwtEnvelope` (`CoseHeaderPolicy.cwt()`: kid protected, no chain, tags 18/61; claims iss/exp/nbf/iat/169 with claim 169 as the PixelPass CBOR byte string; validity from typed `certify.signing.cwt.*`, defaults = keymanager's 180/0 days). Guarded by the claim-169 golden (JCA against the JWKS key) and a JCA-verified unit test; ArchUnit kernel store down 10; full suite 879/879. With this, every credential and token Certify signs goes through `certify-signing`; keymanager DTOs remain only in `SystemInfoController` (administration) and the dead `Credential.addProof` default. |
| 2026-09-19 | P0-03 Signature vectors (claim-169 CWT) | done (slice 4) | `wp/p0-03-vectors-cwt` | Golden for the QR path the compose `FarmerCredential` uses: an `ldp_vc` configuration with `qrSettings` + `qrSignatureAlgo=EdDSA`; the template embeds `claim_169_values`; the test decodes the PixelPass string, checks CWT tag 61 around COSE_Sign1 tag 18, protected `[alg, kid]`, claims `[iss, exp, nbf, iat, 169]` with claim 169 as `bstr .cbor` of the mapped map, and verifies the Ed25519 signature with JCA against the `jwks.json` key named by `kid`. Also fixed `certify-service/pom.xml`: the explicit `kotlin-stdlib:1.8.20` pin made every QR issuance fail with `NoClassDefFoundError: kotlin/text/HexExtensionsKt` (see findings). Stable over two replays; full suite 878/878. Every keymanager signing path now has a golden with independent verification. |
| 2026-09-18 | P1-14 certify-cli (first cut) | done (first cut) | `wp/p1-14-cli` | picocli module and shaded jar: `keys generate/list/jwks` over a PKCS#12 file (`jca` provider), `sign jws` (compact or RFC 7797 detached, typ/kid/x5c/x5t options, PS256 over RSA) and `sign cose` (COSE_Sign1 IssuerAuth layout, optional tag 18 and kid). In-process tests verify every output with Nimbus or JCA. Not yet: LD proofs, issue/batch/verify, keymanager provider. Jar is 53 MB (JSON-LD stack inside certify-signing) — split later. |
| 2026-09-18 | P1-04 KeyPublisher | done | `wp/p1-04-key-publisher` | `jwks.json` and `did.json` are built from `KeyProvider` descriptors (`KeyPublisher` bean; new `KeyProvider.publicKeys(KeyRef)` for every key an alias has had). Goldens for both documents recorded first (key material masked) and unchanged after the switch; JWKS field set per key identical to before; Ed25519 DID method no longer needs a BouncyCastle key class. `JwksServiceImplTest`/`DIDDocumentUtilTest` rewritten against fake providers; ArchUnit kernel store down 31; full suite 877/877. Remaining `io.mosip.kernel` users in `certify-service`: `SystemInfoController` (keymanager administration), `Credential.signQRData` (CWT), `Credential.addProof` (`vc`). |
| 2026-09-18 | P1-03 envelope routing (slice 3) | done (slice 3) | `wp/p1-03-envelope-routing-3` | `MDocProcessor.signMSO` builds the ISO 18013-5 IssuerAuth through `CoseEnvelope.sign1` with `CoseHeaderPolicy.mdocIssuerAuth()` (same layout keymanager produced: protected `alg`, unprotected `x5chain`, untagged); `CoseSignatureService` gone from the processor. Guarded by the mDoc golden (JCA verification against the x5chain leaf) and JCA-verified unit tests replacing the keymanager stubs; ArchUnit kernel store down 10; full suite 880/880. Left on keymanager DTOs: claim-169 `cwtSign` in `Credential.signQRData`, `Credential.addProof` (`vc`), `JwksServiceImpl`/`DIDDocumentUtil` (P1-04), `SystemInfoController`. |
| 2026-09-18 | P0-03 Signature vectors (mDoc) | done (slice 3) | `wp/p0-03-vectors-mdoc` | `mso_mdoc` golden: an mDL configuration (`docType`, `msoMdocClaims`, ES256 with `CERTIFY_VC_SIGN_EC_R1`) issued with a `did:jwk` holder proof; the IssuerAuth COSE_Sign1 is verified with plain JCA against the `x5chain` leaf (which is also a JWKS key), the MSO decoded with the CBOR library and checked structurally (docType, digests, validityInfo, device key = holder key); goldens for the response shape and an MSO summary (protected `alg -7`, unprotected `[33]`, untagged). Note for P1-03: this layout equals `CoseHeaderPolicy.mdocIssuerAuth()`. Stable over two replays; full suite 879/879. Still unguarded: claim-169 `cwtSign`, `Credential.addProof` (`vc`). |
| 2026-09-18 | P1-03 envelope routing (slice 2) | done (slice 2) | `wp/p1-03-envelope-routing-2` | `Ed25519Signature2018`, `EcdsaSecp256k1Signature2019` and `EcdsaKoblitzSignature2016` generators now produce their detached JWS through `LdLegacyEnvelope.detachedJws`; `AccessTokenJwtUtil` signs through `JwsEnvelope` (header `alg`, `kid` as before). Guarded by the slice-2 goldens (danubetech verification of the 2018/k1 proofs, Nimbus verification of the access token) and JCA-verified unit tests replacing the mock-passthrough ones; ArchUnit kernel store down another 55 references; full service suite 878/878. Remaining on keymanager DTOs: mDoc `coseSign1`, claim-169 `cwtSign`, `Credential.addProof`, `JwksServiceImpl`/`DIDDocumentUtil` (P1-04), `SystemInfoController`. |
| 2026-09-18 | P0-03 Signature vectors (second slice) | done (slice 2) | `wp/p0-03-vectors-slice-2` | Goldens now also lock `Ed25519Signature2018` (danubetech `Ed25519Signature2018LdVerifier`), `EcdsaSecp256k1Signature2019` (danubetech verifier over a JCA `ByteVerifier`), `EcdsaSecp256r1Signature2019` (danubetech canonicalization + JCA, because no third-party verifier exists for this suite; see findings) and the pre-authorized code flow (`/pre-authorized-data` → `/credential-offer-data/{id}` → `/oauth/token`, access token verified with Nimbus against `jwks.json`; goldens for the offer, the token response and the token claims and header). Metadata golden re-recorded because three configurations were added (fixture change, not behaviour). Still unguarded: mDoc `coseSign1`, claim-169 `cwtSign`, `Credential.addProof` (`vc`). Stable over two replays. |
| 2026-09-18 | P1-03 envelope routing (slice 1) | done (slice 1) | `wp/p1-03-envelope-routing` | The golden-guarded signing paths now sign through `KeyProviderRegistry` + `certify-signing`: `Ed25519Signature2020` and `EcdsaSecp256r1Signature2019` generators (`LdLegacyEnvelope.proofValueBase58`), `RSASignature2018` (`LdLegacyEnvelope.detachedJws`, RFC 7797 with `kid`/`x5t#S256`), the Data Integrity branch of `W3CJsonLD` (`SignerByteSigner`), `SDJWT.addProof` (`JwsEnvelope` with `JwsHeaderPolicy.sdJwtVc()`). `LegacyKeyRefs` maps `key_manager_app_id`/`ref_id` to `keymanager:APPID/REFID`; `KeymanagerByteSigner` deleted. `JwsEnvelope` fixed to assemble unencoded binary payloads as bytes. Mock-passthrough unit tests replaced by JCA/Nimbus-verified ones. Goldens unchanged (danubetech and Nimbus verification green); ArchUnit kernel store down another 57 references; full service suite 881 tests, 0 failures, 1 skipped. Slice 2 (ES256K legacy suites, mDoc COSE, claim-169 CWT, access tokens, `Credential.addProof`) waits for vectors; see the WP spec. |
| 2026-09-18 | P1-02 keymanager KeyProvider | done | `wp/p1-02-keymanager-provider` | New module `certify-keyprovider-keymanager`: `KeymanagerKeyProvider` (resolve by latest valid certificate or pinned key id, `signRaw` via `signv2` base64url = JOSE raw signature, `publicKeys` from the key-alias-mapper aliases, `ensureKeys` reproducing `initKeys`), `KeymanagerAlias` grammar `APPID/REFID`, `KeymanagerKeyInitializer` (master keys always; signing keys in DataProvider mode: the historical four plus mapper extras), `KeymanagerKeyProviderConfiguration` (a Boot auto-configuration) owning the kernel component scan and JPA wiring. `CertifyServiceApplication` no longer scans kernel packages; `AppConfig` lost its keymanager code. Lesson kept in the WP spec: module wiring goes through auto-configuration, not `@Import` on the application class, or `@WebMvcTest`/`@DataJpaTest` slices break. `SigningKey.withAlgorithm` for PS256 over RSA. 16 module tests against a keymanager stand-in (Nimbus-verified for five algorithms) and a wiring test booting the real service (H2 + PKCS#12) proving the provisioned keys sign verifiably and publish the same kids as the JWKS endpoint. Goldens unchanged; ArchUnit kernel store down 39 references; full service suite 893/893. Signing call sites still use keymanager directly until P1-03. |
| 2026-09-18 | P1-01 certify-spi + issuance core | done | `wp/p1-01-spi-and-issuance-core` | New modules `certify-spi` (27 types: configuration model, `IssuanceContext`, `HolderBinding`, `ClaimSet`, `Unsigned`/`IssuedCredential`, `TenantContext`, and the `CredentialFormatter`, `CredentialDataSource`, `ExternalIssuer`, `ProofValidator`, `StatusProvider`, `TemplateEngine`, `IssuanceListener` interfaces) and `certify-issuance` (`IssuanceCommand` with draft-13 selector, sealed `IssuanceResult`, `DefaultIssuanceService` running the eight-step flow, `FormatterRegistry`, `KeyProviderRegistry`, `AuthorizationPolicy.SCOPE`, `InMemoryConfigurationRegistry`). 23 component tests with fakes and the real `JcaKeyProvider`; the issued JWS verifies with Nimbus; ArchUnit keeps spi/issuance/signing free of Spring, JPA, Velocity, keymanager and `certify-core`. Not yet wired into `certify-service`. Named `certify-issuance` because the `certify-core` artifact is the existing DTO module (decision logged). |
| 2026-09-18 | P0-03 Signature vectors (first slice) | partial | `wp/p0-03-signature-vectors` | Signing paths now locked by end-to-end goldens with independent verification: `Ed25519Signature2020` (legacy multibase, danubetech), `eddsa-rdfc-2022` (Data Integrity over VC 2.0, danubetech `DataIntegrityProofLdVerifier`), `RsaSignature2018` (detached JWS, danubetech), SD-JWT `jwsSignV2` ES256 (Nimbus against `jwks.json`). Still unguarded: `EcdsaSecp256r1Signature2019`, ES256K suites, mDoc `coseSign1`, claim-169 `cwtSign`, access-token `jwsSign`; to add before P1-03 touches those paths. |
| 2026-09-18 | P0-01 Golden tests (v1) | done (first slice) | `wp/p0-01-golden-tests-v1` | `IssuanceGoldenTest` boots the real service in the test JVM (H2, PKCS#12 keymanager, Velocity, keymanager signing; profiles `local,test`), creates an `ldp_vc` (Ed25519Signature2020) and a `dc+sd-jwt` (ES256) configuration, fetches a nonce, builds a holder proof and issues through `POST /issuance/credential`; the ldp_vc verifies with danubetech against `did.json`, the SD-JWT with Nimbus against `jwks.json`; normalized goldens under `goldens/legacy-develop` (metadata, both credentials, two error bodies), stable over three runs. Fixed the stale H2 `credential_config` schema (missing `signature_crypto_suite`, `credential_status_purpose`, `qr_*`, claim columns; wrong PK). Remaining paths (Data Integrity, RSA, ES256K, mDoc, claim-169 CWT, access tokens) follow as P0-03. |
| 2026-09-18 | P0-08 CredentialRegistry | done | `wp/p0-08-credential-registry` | `CredentialRegistry` (core SPI) with `CredentialRegistryImpl` caching the issuer-metadata view in `issuerMetadataCache` (programmatic `CacheManager`, evicted by `@CacheEvict` on every config write); `CertifyIssuanceServiceImpl`, `VCIssuanceServiceImpl`, `PreAuthorizedCodeService` and `WellKnownController` read through it, so a credential request no longer runs `findAll()`; `VCIssuanceUtil.toCredentialConfigurationSupported` extracted. |
| 2026-09-18 | P0-07 AuthorizationContext | done | `wp/p0-07-authorization-context` | `AuthorizationContext` (plain, tenant-aware, in `certify-core`) as a request-scoped bean; `ParsedAccessToken` is now a stateless delegate bean (`RequestScopedParsedAccessToken`, `@Primary`) so filters, services and plugins are thread-safe without code changes and fall back to local fields outside a request; `ParsedAccessToken` deprecated for plugins. Two-thread isolation test. Also fixed a regression from P0-09: `@WebMvcTest` slices have no `MeterRegistry`, so the interceptor now resolves it lazily. Full suite: 877 tests, 0 failures. |
| 2026-09-18 | P0-04 Flyway baseline + P0-05 Testcontainers | done | `wp/p0-04-flyway-baseline` | `flyway-core` with per-module locations `db/migration/{core,keymanager,verify,as}` and baselines `V1_0_0_000..003` generated from `db_scripts` by `db_scripts/generate_flyway_baseline.sh`; `FlywayDefaults` (baseline-on-migrate at `1.0.0.003`, schema from `currentSchema`, locations) applies unless `spring.flyway.*` is set; `io.mosip.certify.tools.MigrateOnly` entry point (`-Dloader.main=...`) and a Helm pre-install/pre-upgrade Job (`dbMigration.enabled`); H2 test profiles disable Flyway. `FlywayMigrationTest` on Testcontainers PostgreSQL 15: empty DB gets 15 tables from the baselines, a `db_scripts` DB is baselined with zero migrations, and both roads produce identical columns, indexes and constraints. Needed Testcontainers 1.21.4 (Docker Engine 29 removed API < 1.44) and docker-java 3.5.3. |
| 2026-09-18 | P0-10 Signing skeleton | done | `wp/p0-10-signing-skeleton` | New modules `certify-signing` (`SignatureAlgorithm`, `AlgorithmRegistry` with the JOSE/COSE/LD-suite/cryptosuite table, `KeyRef`, `CertificateChain`, `PublicKeyDescriptor` with JWK export, `SigningKey`, `KeyProvider`, `Signer`, `KidStrategy` PROVIDER/JWK_THUMBPRINT/X5T_S256, `JwsHeaderPolicy`, `CoseHeaderPolicy`, `JwsEnvelope`, `CoseEnvelope`, `CwtEnvelope`, `KeyPublisher`) and `certify-keyprovider-jca` (PKCS#12 load/save, dev mode generating ES256/EdDSA/RS256/PS256/ES256K keys with self-signed certificates, `ensureKeys`). 21 tests: every JWS verified by Nimbus, every COSE_Sign1 by plain JCA. Data Integrity and legacy LD envelope builders arrive with P1-03 (they need the danubetech stack). Not yet wired into `certify-service`. Also fixed the P0-09 interceptor constructor ambiguity that broke context boot. |
| 2026-09-18 | P0-09 Deprecation infrastructure | done | `wp/p0-09-deprecation-infra` | `@DeprecatedEndpoint` + `DeprecationInterceptor`: RFC 9745 `Deprecation`, RFC 8594 `Sunset`, `Link rel=deprecation/successor-version`, Micrometer `certify.deprecated.calls{endpoint,enabled}`, hourly log throttle, kill switch `mosip.certify.deprecated.<name>.enabled=false` answering 410 JSON; OpenAPI `deprecated` flag when springdoc is present. No endpoint annotated yet. |
| 2026-09-18 | P0-06 ArchUnit rules | done | `wp/p0-06-archunit-rules` | Six frozen rules; store holds 246 `io.mosip.kernel` references outside a provider module, 5 Spring Web uses in `certify-core`, 4 repository reads in the template engine; controllers and VCFormats rules currently clean (VCFormats constants are inlined by javac, so that rule bites only after P1) |
| 2026-09-18 | Baseline | done | `design/extensibility` | Existing suite on JDK 21: 858 tests, 0 failures, 1 skipped, BUILD SUCCESS. Build needs `JAVA_HOME` on JDK 21, `-Dgpg.skip=true`, and locally `.mvn/settings-local.xml` because JitPack hangs on missing coordinates; see CLAUDE.md |

## Findings about the existing test suite ("trust but verify")

- Status-list assignment cannot run on H2: `StatusListCredentialService.initializeAvailableIndices` uses a native PostgreSQL `generate_series` insert, so every existing test of the feature mocks the service and no test issues a credential with a real status entry. Covered since P1-10a by `StatusListPostgresTest` (Testcontainers). The H2 `schema.sql` now has the three status tables; an end-to-end status golden needs the Testcontainers PostgreSQL context from P0-05.
- Status list credentials were signed with `Ed25519Signature2020` under the VC 2.0 context alone, which defines `DataIntegrityProof` only: the proof `type` expanded to an undefined term, so strict verifiers rejected every list (danubetech accepted them because signer and verifier drop the same triple). Fixed in P1-10a for `Ed25519Signature2020` (suite context added); `Ed25519Signature2018` lists (compose `certify-mock-mdl.properties`) keep the defect because the context loader serves no `ed25519-2018` context. The same applies to any VC 2.0 template signed with a legacy suite whose context the template omits; output validation on the new surface should reject such documents.
- `POST /credentials/status` takes the bare status list id in `credentialStatus.statusListCredential` (and `id`), while the credential carries the list URL; a caller that forwards the credential's own `credentialStatus` gets `STATUS_LIST_NOT_FOUND`. Kept as is on the compatibility surface; the `StatusProvider.update` path (P5) should accept the URL.
- The Flyway chain creates `key_policy_def` but carries none of its rows; they come from `db_scripts/inji_certify/dml` through `init_db.sh`. A database prepared by Flyway alone cannot start the keymanager (`initKeys` needs the policies). `StatusListPostgresTest` seeds the CSV itself; a DML migration (`INSERT ... ON CONFLICT DO NOTHING`) needs a decision because it would also run on baselined databases.
- `application-test.properties` sets `mosip.certify.status-list.vc-sign-algo`, a key nothing reads; the service reads `mosip.certify.status-list.signature-crypto-suite`.
- 0.14.0 findings from the draft-13 recording (`P0-02-notes.md`): its mDoc `validityInfo` lacks `signed` (ISO/IEC 18013-5 9.1.2.4) and wraps the credential as a Document with a tagged `issuerAuth`; an unknown metadata version answers HTTP 200 with the MOSIP `errors[]` envelope; its H2 test schema predates its own `CredentialConfig` entity, so no 0.14.0 test inserted a configuration through the repository; the token response and access token carry `c_nonce`, which develop dropped without a replacement on the token side.
- On H2 a `TEXT[]` column read back as one stringified element is re-stringified on every write, so `cryptographic_binding_methods_supported` of the mDoc configuration deepens (`[[[cose_key]]]`) with every test that touches the row; the depth depends on test order (the first P1-12 slice was red on CI for this while green locally). `Goldens.collapseBrackets` folds any depth to one pair. On PostgreSQL the column is a real array and the bug does not occur.
- `Goldens.normalize` sorted object arrays by their raw string before sorting keys, so a recorded golden did not re-normalise to itself; fixed (key-sorted comparison). Only array order in the v1 `did.json` and `jwks.json` goldens changed.
- `StaticContextLoaderTest.maxEntries_full_expiredEntriesPurged_allowsNewInsert` was flaky: a 1 ms cache TTL raced the loader's constructor preload, so the entry sometimes expired before the first load and `resourceLoader.getResource` was invoked twice (`TooManyActualInvocations`). It slipped one merge through (P1-06) because the local gate reads the suite summary after the run; the test now uses a 200 ms TTL and a 400 ms wait (full suite 892/892 after the change). A `Clock` injected into `StaticContextLoader` would make it fully deterministic.
- QR (claim-169) issuance was broken on `develop` as built from this pom: `certify-service/pom.xml` pinned `kotlin-stdlib` to 1.8.20 while `pixelpass-jar` 0.8.0 (and every other Kotlin artifact via Spring Boot) needs 1.9.22, so `PixelPass.generateQRData` threw `NoClassDefFoundError: kotlin/text/HexExtensionsKt` and any configuration with `qr_settings` (the compose FarmerCredential) answered 500. The existing unit tests mock PixelPass and keymanager and never reached it; the new golden did. Fixed by letting Spring Boot manage the version.
- The H2 `schema.sql` for `credential_config` had drifted from the DDL (no `signature_crypto_suite`, `credential_status_purpose`, `qr_settings`, `qr_signature_algo`, `mso_mdoc_claims`, `sd_jwt_claims`; `vct` instead of `sd_jwt_vct`; composite PK) because no test ever inserted a configuration; fixed in P0-01.
- The test profile's signing properties are placeholders (`TestSignature`, `TEST_APP_ID`), so nothing in the old suite exercised a real signing path end to end.

## Spec and behaviour findings from the goldens (to fix in the adapters, not silently)

- `W3CJsonLD.addProof` derives the proof's `created` by parsing the credential's `issuanceDate`/`validFrom` (a `Z` timestamp) with `LocalDateTime.parse(...).atZone(ZoneId.systemDefault())`, so on a JVM outside UTC `created` is shifted by the local offset (5:30 h on IST). The `ldp_vc` formatter reads the timestamp as UTC; the legacy path keeps the shift until the adapter switch (zero wire change).
- The claim-169 CWT carries `kid` but no `x5chain`: `Credential.signQRData` asks keymanager with `protectedHeader {x5c: true, kid: true}` but keymanager's `CoseHeaderBuilder` only honours its own keys, so the certificate is never included and a verifier must resolve the key from `jwks.json` by `kid`. Keymanager also adds `exp`, `nbf` and `iat` with its own defaults. The `CwtEnvelope` route should include the chain and take the validity from configuration.
- `did.json` lists the DID itself in `assertionMethod` and `authentication` instead of verification-method ids (DID Core §5.3.1 expects references to verification methods); `alsoKnownAs` and `service` are emitted as empty arrays. Verifiers that resolve `verificationMethod` directly still work, which is why the credentials verify.
- `EcdsaSecp256r1Signature2019` (P-256, `proofValue` base58btc) is Certify's own suite name: it is not in the W3C LD cryptosuite registry and no library outside Certify verifies it (danubetech has k1 and Ed25519 only). Spec-correct P-256 issuance is `ecdsa-rdfc-2019` (Data Integrity), which Certify already supports; the legacy suite should be deprecated on the compatibility surface.
- `POST /pre-authorized-data` answers HTTP 200 with `{"errors":[{"errorCode":"unknown_claims"}]}` (legacy `ResponseWrapper` envelope) instead of an OAuth-style 4xx; the new `/oid4vci` surface must not inherit this.
- The credential offer's `credential_issuer` is `mosip.certify.identifier` (with servlet path) while the issuer metadata's `credential_issuer` is `mosip.certify.domain.url`; OpenID4VCI requires the same Credential Issuer Identifier in both (second face of the dual-identity finding above).
- The access token's `sub` is the offer's claims map serialized as JSON in `HashMap` order (PII inside the token, nondeterministic), `client_id` is the empty string, and `aud` is built by string concatenation (`${mosip.certify.identifier}/v1/certify/issuance/credential`), which duplicates the servlet path whenever the identifier already carries it (as in the test profile).
- Issuer metadata orders `credential_metadata.claims` by `HashMap` iteration, so the document differs between JVM runs; the order should follow the configured display order.
- Optional display fields absent from a configuration are serialized as `null` (`logo`, `background_color`, `background_image`, `text_color`, `claims[].display`); OpenID4VCI defines them as optional objects and strings, so they should be omitted (`@JsonInclude(NON_NULL)` on `MetaDataDisplayDTO` and `CredentialMetadataDTO.Claims`).
- `mosip.certify.identifier` (proof audience, SD-JWT `iss`) and `mosip.certify.domain.url` (`credential_issuer` in metadata) are two keys for one identity; in the test profile they already disagree (`.../v1/certify` vs `http://localhost:8090`). See docs/design/14-configuration.md.
- `credential_definition.type` is emitted sorted alphabetically (`["GoldenCredential","VerifiableCredential"]`) because the mapper stores types as a sorted comma list; the issuer's configured order is lost.
- On H2, `TEXT[]` columns come back as one stringified element (`"[did:jwk, did:web]"`); PostgreSQL returns a proper array. One more reason the repository tests must run on PostgreSQL (P0-05).

- `CertifyApplicationTests.test()` boots the application a second time by calling `CertifyServiceApplication.main()` inside a `@SpringBootTest`; it asserts only that the class is not null.
- Controller tests are `@WebMvcTest` with mocked services; no test exercises an end-to-end issuance through the real signing path.
- Repository tests run on H2 with `schema.sql`, not on the PostgreSQL DDL under `db_scripts` (JSONB, `TEXT[]`, enum and partial indexes are never exercised).
