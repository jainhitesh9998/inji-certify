# Risks, open questions and decisions needed

> Part of the Inji Certify extensibility design. Baseline: `develop` at `a1cfd63` (1.0.0-beta.1-SNAPSHOT). Index: [README.md](./README.md).

The two largest risks are shipping develop to a deployment whose wallets still speak draft 13, and changing signature bytes while wrapping keymanager; both are closed by Phase 0 artefacts that exist before any refactor starts.

| Risk | Impact | Mitigation |
| --- | --- | --- |
| A develop-based release reaches wallets that speak draft 13 | Issuance stops for those wallets | Confirm which wallet versions are in production before the first develop release; ship `oid4vci-d13` in P1 and default it on where such wallets exist |
| Signature bytes change while routing keymanager through the envelope builders | Verifiers with pinned expectations, cached `kid` values or `x5t` checks reject credentials | Golden signature vectors per path recorded in P0; ECDSA compared by verification; the keymanager provider calls keymanager's native envelope methods rather than `signRaw` |
| Extracting keymanager's wiring out of `CertifyServiceApplication` and `AppConfig` breaks its auto-configuration | Service fails to boot or keys are not found | The provider module reproduces the exact package list; an integration test boots only the provider against the keymanager tables |
| Golden tests miss behaviour a wallet relies on (error bodies, header casing) | Silent breakage in P1 | Record goldens from real Inji Wallet and mimoto traffic as well as hand-written cases; weekly wallet interop from P0 |
| Keymanager stays the only provider in practice and its DTOs leak back across the seam | The signing SPI becomes decorative | ArchUnit rule from P0; the `jca` provider in the test kit from P1; PKCS#11 in P5 |
| Tenant columns and index rebuilds on large `ledger` tables | Long migration lock on big deployments | Add `tenant_id` with a default and build indexes `CONCURRENTLY` in a separate migration step; rehearse on a production-sized dump |
| `verify-core` and keymanager tables in the `certify` schema drift with their own upgrades | Migration conflicts between three owners | Per-module Flyway locations from P0; verify tables owned by `certify-as` |
| HAIP's PAR and wallet attestation belong to the AS, which in production may be eSignet | P4 effort spent on `certify-as` that deployments will not use | Decide the target AS before P4; if eSignet, P4 ships the issuer half plus a written AS contract |
| Dual-write of config v1 and v2 drifts | Wrong metadata for some configurations | One write-through service; the migration-verifier test compares legacy and JSONB reads for every row |
| Plugin authors stay on `certify-integration-api` | Legacy adapters cannot be removed in 2.0.0 | Two-release guarantee, `certify-spi-testkit`, a mechanical mapping table in the migration guide |
| Presentation during issuance (IAE) is a draft extension that 1.0 may not carry | Core shaped around an unstable flow | Keep IAE in `certify-as` behind a feature flag; the core never depends on it |
| Re-layering turns into feature work | P1 slips and cannot be released | Rule: P1 ships zero behaviour change; new capability only from P3 |
| CI has no Docker for Testcontainers | Database tests cannot run per PR | Confirm the `kattu` workflows' runners; fall back to a PostgreSQL service container |
| A CLI run with the keymanager provider needs the keymanager database and HSM | Offline signing is not possible with keymanager keys | Documented; offline runs use the PKCS#11 or PKCS#12 provider |

Decisions taken on 2026-09-18 (details in the log below):

- [x] Draft-13 wallets: `oid4vci-d13` is built in P1 and on by default, deprecated from day one.
- [x] Issuer surface: a new spec-clean surface under `{domain}{servletPath}/oid4vci`; today's paths kept as a deprecated compatibility surface.
- [x] Keymanager: kernel-keymanager stays the embedded default provider; other key managers plug in above it; no remote keymanager.
- [x] Where work lands: branches on `jainhitesh9998/inji-certify` only, integration branch `design/extensibility`; no pull requests to `inji/inji-certify`.
- [x] Flyway and CI: Helm pre-upgrade Job in Kubernetes, startup migration in docker-compose; GitHub Actions on the fork runs the gates with Docker.
- [x] HAIP: both halves; Certify as a HAIP resource server behind any HAIP-capable authorization server, and `certify-as` implementing PAR, PKCE, DPoP-bound tokens and wallet attestation itself.
- [x] First non-keymanager provider: `x509-file` (PKCS#12 or PEM) with a dev mode that generates a key and a test chain at startup.
- [x] Tenant isolation: shared schema with a defaulted `tenant_id` column.
- [x] Templating: Velocity stays the default for existing whole-document templates; SD-JWT and mDoc use a standard claims mapping with format libraries building the envelope; a JSON mapping engine and a no-template path are provided; output validated by JSON Schema per format plus format-specific checks.
- [x] Status for SD-JWT and mDoc: IETF Token Status List; Bitstring stays for W3C credentials.
- [x] `verify-core` tables: stay in the `certify` schema unrenamed, created only by the `certify-as` Flyway location.
- [x] Presentation during issuance (IAE): a supported feature kept in `certify-as` behind a feature flag; the core never depends on it.

## Decision log

Record every decision taken while building, newest first. A work package that needs a decision not listed here stops and asks.

| Date | Decision | Options considered | Chosen | By | Affects |
| --- | --- | --- | --- | --- | --- |
| 2026-09-19 | Tenancy resolves by request host first (`certify.tenancy.resolver=host`, `tenants.<id>.hosts`); the `/t/{tenant}` path resolver waits for path routing; a tenant may override the issuer identifier, DID and key namespace, and the new surface appends its `/oid4vci` suffix to a tenant identifier. Off by default, resolver fixed to `default` | host; path prefix; issuer identifier from the token | host first | design (05, 14) | `io.mosip.certify.tenancy`, adapters, `JpaConfigurationRegistry` |
| 2026-09-19 | The 1.1.0 migration ships now (P2-01) as designed in 08-database.md: additive columns and tables, backfill in the same migration, unique indexes of `credential_config` and `ledger` rebuilt once to lead with `tenant_id`; `credential_template` keys on `(id, version)` with the tenant as a column (rule 4); backfilled rows keep `issuance_strategy = TEMPLATE` and the registry keeps deriving the strategy from `mosip.certify.plugin-mode` until the v2 API writes the column; a `provider:alias` key column (P3-01) is backfilled to its own provider | wait for the entities and the v2 API to land first; ship the schema first and switch readers later | schema first | design (08-database, roadmap P2 ordering) | `db/migration/core`, `db_upgrade_script`, `FlywayMigrationTest` |
| 2026-09-19 | The compatibility path `POST /issuance/credential` is served by the new core behind `certify.protocol.oid4vci-v1.compat-core.enabled` (off by default, DataProvider plugin mode only) with the v1 goldens as the proof in both modes; the default flips and `CertifyIssuanceServiceImpl` is deleted in a follow-up once CI has run both modes. Legacy template-model parameters, the render-method digest and claim-169 QR signing live as `beforeRender` listeners (new SPI hook) rather than in the engine or the formatters | switch now and delete; flag first; keep the legacy service for good | flag first | upgrade safety (non-negotiable 3) | `VCIssuanceController`, `certify-spi`, `certify-issuance`, v1 goldens |
| 2026-09-19 | Versioned draft-13 metadata is served only with an explicit `?version=` (and under the `/issuance/.well-known/` aliases); the unversioned `/.well-known/openid-credential-issuer` stays the OpenID4VCI 1.0 document, as on develop. An unknown version keeps 0.14.0's HTTP 200 error envelope on this compatibility surface. The draft-13 documents present SD-JWT configurations as `vc+sd-jwt` whatever the row stores (`dc+sd-jwt` on develop), the only format string those wallets know | serve the draft-13 shape by default (breaks 1.0 wallets); versioned only | versioned only | api compatibility (09) | `D13DiscoveryController`, `goldens/legacy-0.14.0/well-known` |
| 2026-09-19 | The draft-13 body on `/issuance/credential` is dispatched by a request condition (`D13BodyBufferFilter` marks bodies with `format` and no `credential_configuration_id`; `D13BodyCondition` ranks the draft-13 handler first for them), leaving `VCIssuanceController` untouched | change the 1.0 controller to read a raw body and delegate; rewrite the path in a filter (breaks the token filters' URL lists and MockMvc); custom request condition | custom request condition | judgement | `oid4vci.d13`, P1-13 move |
| 2026-09-19 | The draft-13 adapter reproduces the 0.14.0 mDoc wire shape a wallet parses (Document `{docType, issuerSigned}`, `issuerAuth` under COSE tag 18) but keeps the tag-24 MSO wrapper and `validityInfo.signed` that ISO/IEC 18013-5 requires and 0.14.0 lacked; the replay folds the two documented deviations back before comparing | reproduce 0.14.0 byte for byte (ships a known spec defect on a new surface); keep the core's spec-correct MSO | keep the spec-correct MSO | spec correctness (non-negotiable 1) | `D13Mdoc`, `goldens/legacy-0.14.0/issuance/mso_mdoc-summary` |
| 2026-09-19 | A draft-13 `vc+sd-jwt` request is answered with `typ vc+sd-jwt` (0.14.0 wallets check it); every other surface issues `dc+sd-jwt`. The draft-13 endpoints carry `@DeprecatedEndpoint` from their first release (`since` 2026-09-19, replacement `/oid4vci/credential`) | one typ everywhere; per-request typ | per-request typ, deprecated from day one | roadmap (draft-13 on by default, deprecated from day one) | `SdJwtFormatter`, `certify.deprecated.calls` |
| 2026-09-19 | Status list credentials carry the JSON-LD context of the proof suite they are signed with (`https://w3id.org/security/suites/ed25519-2020/v1` for `Ed25519Signature2020`). The VC 2.0 context defines `DataIntegrityProof` only, so without it the proof type expands to an undefined term and strict verifiers reject the list. Newly generated lists change on both surfaces; stored lists stay as they are until regenerated. | leave as develop (verifies only with lenient verifiers); switch the default suite to `eddsa-rdfc-2022` (changes every deployment's default); add the suite context | add the suite context | spec correctness (non-negotiable 1) | `StatusListCredentialService`, status list wire bytes, `goldens/oid4vci-1.0/status-list` |
| 2026-09-19 | Until the configuration model gets its own signing columns (P2 migration), a `credential_config.key_manager_app_id` of the form `provider:alias[@version]` names a non-keymanager `KeyProvider` on the new surface; plain values keep meaning keymanager's application id | new column now; prefixed value | prefixed value | judgement | P3 providers, JpaConfigurationRegistry, P2 migration |
| 2026-09-19 | The new surface's Credential Issuer Identifier is `mosip.certify.identifier` + `/oid4vci` (overridable with `certify.oid4vci.issuer-identifier`); its metadata is served at `{identifier}/.well-known/openid-credential-issuer` like today's, and proofs must name it as `aud` | same identifier as the old surface; own identifier | own identifier | judgement (design 05, decision of 2026-09-18) | oid4vci adapter, wallets, nginx mapping |
| 2026-09-19 | The `/oid4vci` adapter lives in `certify-service` (package `io.mosip.certify.oid4vci`) until P1-13 splits the service; it depends on service beans (`AuthorizationContext`, `VCICacheService`, plugins) that have no module yet | separate module now with a circular dependency; inside the service | inside the service | judgement | P1-11, P1-13 |
| 2026-09-19 | The new surface secures itself: its own `SecurityFilterChain` for `/oid4vci/**` and token filters subclassing the service's (profile-bound) access-token filters; no entry is added to `ignore-auth-urls`/`filter-urls` (rule 9) | URL lists; adapter chain | adapter chain | judgement | oid4vci adapter, later adapters |
| 2026-09-19 | Goldens of the new surface live under `goldens/oid4vci-1.0/<adapter>`; they are recorded, not compared with v1 (the new surface is allowed to differ where the spec requires) | reuse v1 goldens; separate v2 | separate v2 | judgement | goldens, P3 |
| 2026-09-19 | The new core reads `credential_config` unchanged through `JpaConfigurationRegistry`; the draft-13 selector for `ldp_vc` is `context|credentialType` as stored, SD-JWT selects by vct, mDoc by docType; the status mechanism of existing rows is `BitstringStatusList`; the issuance strategy follows `mosip.certify.plugin-mode` (TEMPLATE for DataProvider, EXTERNAL for VCIssuance) | new table/columns now; read the existing rows | read existing rows | judgement | P1-11, P2 migrations, CLI |
| 2026-09-19 | The second template engine is `jsonmap`: templates are JSON data with `${...}` placeholders and are never executed; the new core picks the engine from `TemplateRef.engine` (`velocity` default, `jsonmap`, or mode `NONE` for claims as-is) | Velocity only; a second scripting engine (Handlebars, Freemarker); a data-only engine | data-only `jsonmap` | judgement | certify-template-jsonmap, P1-05.., configuration model |
| 2026-09-19 | The claim-169 CWT keeps keymanager's layout on the compatibility surface (kid in the protected header, no `x5chain`, `exp`/`nbf` from `certify.signing.cwt.*` defaulting to 180/0 days); adding the chain is a wire change done on the new surface | add x5chain now; keep layout | keep layout | judgement | QR verifiers (resolve by kid from jwks.json), P3 |
| 2026-09-18 | `KeyProvider` gains `publicKeys(KeyRef)`: every key an alias has had (current included) so DID documents and JWKS keep rotated-out keys resolvable; providers without history return the current key | filter by purpose only; alias-scoped listing | alias-scoped listing | judgement | certify-signing SPI, P1-04, DID/JWKS publishing |
| 2026-09-18 | Legacy LD suites and Data Integrity share danubetech canonicalization in `certify-service` for now; `certify-signing` contributes the proof encodings (`LdLegacyEnvelope`, `SignerByteSigner`) and gains `ld-signatures-java`/`data-integrity-java`; the full LD envelope builder moves with the `ldp_vc` formatter (P1-05) | full builder now; encodings now, builder with the formatter | encodings now | judgement | certify-signing, P1-05 |
| 2026-09-18 | Modules wire themselves into an application through Spring Boot auto-configuration (`META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`), never through `@Import` on the application class or extra `scanBasePackages`; test slices then exclude them the standard way | scanBasePackages; @Import; auto-configuration | auto-configuration | judgement | every provider, formatter and adapter module; CLAUDE.md rule |
| 2026-09-18 | Keymanager keys are addressed as `keymanager:APPID/REFID[@keyId]` (empty reference id omitted); the optional version pins keymanager's key id (certificate thumbprint) so a configuration can hold a rotation back deliberately | separate fields; alias grammar | alias grammar | judgement | KeyRef, credential configuration signing block, CLI |
| 2026-09-18 | Algorithm choice within a key family (RS256 vs PS256 on one RSA key) is made by `SigningConfig.algorithm` and applied through `SigningKey.withAlgorithm` in `KeyProviderRegistry`; providers report a key's natural algorithm and refuse cross-family use | header policy carries alg; key carries alg | key copy with the configured alg | judgement | certify-signing, formatters, P1-03 |
| 2026-09-18 | Startup provisioning creates, besides the historical four signing keys, every alias the key-alias-mapper names for an algorithm keymanager can generate (RS256, PS256, ES256, ES256K, EdDSA); unsupported algorithms are logged and skipped | historical four only; mapper only; both | both | judgement | deployments with custom mappers, P1-02 |
| 2026-09-18 | The keymanager provider cannot be switched off until the direct signing call sites are gone (P1-13); the switch `mosip.certify.key-provider.keymanager.enabled` arrives with that WP rather than as a flag that breaks boot today | flag now; flag later | later | judgement | P1-13, configuration |
| 2026-09-18 | The new protocol-agnostic core module is `certify-issuance`, not `certify-core`: the existing `certify-core` artifact (DTOs, `ParsedAccessToken`, `CredentialRegistry`, Spring Web uses) keeps its name and coordinates so plugins built against it keep resolving; it shrinks over Phase 1 and becomes a compatibility shell | rename existing module and reuse `certify-core`; new name | new name `certify-issuance`; design docs read `certify-core` as this module | judgement | P1-*, plugin compatibility, ArchUnit |
| 2026-09-18 | Work lands on branches of `jainhitesh9998/inji-certify` only; integration branch `design/extensibility`; no PRs to upstream | upstream PRs; integration branch; fork only | fork only | project owner | automation, CI |
| 2026-09-18 | Flyway runs from a Helm pre-upgrade Job in Kubernetes and at startup in docker-compose; the application only validates at boot; CI is GitHub Actions on the fork with Docker for Testcontainers | Job; startup everywhere; manual | Job + compose startup | project owner (judgement delegated) | P0-04, P0-05, CI |
| 2026-09-18 | HAIP on both sides: resource-server behaviour works behind any HAIP-capable AS (eSignet is one of several), and `certify-as` implements PAR, PKCE, DPoP-bound access tokens and wallet attestation | issuer half only; both | both | project owner | P4, certify-as, certify-authz |
| 2026-09-18 | First non-keymanager provider is `x509-file` (PKCS#12 or PEM) with a dev mode generating a key and a test IACA-to-DSC chain at startup | KMS; Vault; file | file with dev mode | project owner | P3, tests, CLI |
| 2026-09-18 | Tenant isolation: shared schema with a defaulted `tenant_id` column; schema- or database-per-tenant stays reachable through Hibernate multi-tenancy later | shared schema; schema per tenant; database per tenant | shared schema | project owner | P2 migration, entities |
| 2026-09-18 | Templating: Velocity remains the default engine for existing whole-document (`FULL_DOCUMENT`) templates; SD-JWT and mDoc use a standard claims mapping (`CLAIMS_ONLY`) with the format libraries building the envelope; a `jsonmap` engine and a no-template build path exist; output validated by JSON Schema per format plus format checks (SD path validity, mDoc namespace shape) | Velocity only; replace Velocity; both | both, standard mapping for SD-JWT and mDoc | project owner (validation by judgement) | P1-08, P5 |
| 2026-09-18 | Status mechanism: IETF Token Status List for SD-JWT and mDoc; Bitstring Status List stays for W3C credentials | Bitstring everywhere; Token Status List | Token Status List for SD-JWT and mDoc | judgement | P5 StatusProvider |
| 2026-09-18 | `verify-core` tables stay in the `certify` schema unrenamed, created only by the `certify-as` Flyway location | rename with prefix; own schema; leave | leave, owned by certify-as | judgement | P0-04 |
| 2026-09-18 | Presentation during issuance (IAE) is a supported feature kept in `certify-as` behind a feature flag; the core does not depend on it | drop; experiment; supported | supported behind a flag | project owner | certify-as |
| 2026-09-18 | Draft-13 support: `oid4vci-d13` adapter built in P1, on by default, deprecated from day one | not supported; built but off; on by default | on by default | project owner | P0-02, P1-12, deprecation counters |
| 2026-09-18 | Protocol surface: new spec-clean OpenID4VCI 1.0 surface under `{domain}{servletPath}/oid4vci` with its own issuer identifier; today's paths kept as a deprecated compatibility surface over the same core | same path only; second issuer URL only; both | new surface plus deprecated old surface | project owner | `oid4vci-v1`, API compatibility, metadata, tenancy prefix |
| 2026-09-18 | Keymanager: MOSIP kernel-keymanager stays an embedded library and the default key provider; no HTTP or remote keymanager option; keymanager already covers HSM backends (PKCS#11, PKCS#12, offline), so Certify's `KeyProvider` SPI is for other key managers (cloud KMS, Vault, file, X.509 providers, other libraries) | embedded library; remote keymanager service | embedded, wrapped as `certify-keyprovider-keymanager` | project owner | signing, database, CLI |
| 2026-09-18 | PKI-based formats (mDoc/mDL, SD-JWT VC with `x5c`) get a separate `x509` provider family, a per-format `CertificateChainPolicy` and trust-anchor publication, usable without keymanager tables | keymanager CSR flow only; separate providers | separate providers plus policy | project owner | signing, mDoc, SD-JWT, database (`trust_anchor`) |
| 2026-09-18 | Multi-tenancy is off by default but the code is tenant-ready (`TenantContext`, defaulted `tenant_id` columns) | none; tenant-ready; full multi-tenant | tenant-ready | project owner | core, persistence, adapters |
| 2026-09-18 | Design baseline is `develop` (`a1cfd63`), not the 0.14.0 release | master; develop | develop | project owner | everything |
| 2026-09-19 | The `notification_id` of an OpenID4VCI 1.0 credential response is the core's transaction id (a UUID), the `issuance_transaction` row is written only for completed issuances (state `ISSUED`, no `PENDING` row yet), the wallet's report needs the same access token (matched by hash) and moves the row to `NOTIFIED`, and rows are purged one day after issuance by default (`certify.protocol.oid4vci-v1.notification.retention`, `purge-interval` one hour); the compatibility surfaces (today's paths and draft 13) neither return `notification_id` nor advertise the endpoint, so their goldens stay unchanged | opaque random id with a separate lookup column; write `PENDING` before signing; advertise the endpoint on draft 13 too | transaction id, `ISSUED` only, new surface only | agent (P3-02), pending owner review | `oid4vci-v1`, persistence |
| 2026-09-19 | The v2 configuration API takes and returns the 1.1.0 JSONB model (`formatConfig` as the raw `format_config` map, `signing` as `provider`/`alias`/`alg`/`cryptosuite`/`didUrl`, `template` as text with a read-only version), mirrors every legacy column on write so the v1 API and the compatibility surfaces keep working, versions templates by content checksum (a changed content is the next version), dry-runs only a body that carries `sampleClaims` (rendered and built through the core, never signed, refused with `400 template_render_failed` and rolled back), and answers errors as `{error, error_description}` with HTTP statuses rather than the v1 wrapper with HTTP 200 | typed per-format DTOs now; always require sample claims; keep the v1 error wrapper | raw map, optional dry run, spec-style errors | agent (P2-06), pending owner review | config API, persistence |
| 2026-09-19 | On the new surface a nonce-endpoint `c_nonce` is consumed when the credential request it authorised succeeds (all proofs of a batch share it; a refused request leaves it valid), `proofs` are capped by `certify.protocol.oid4vci-v1.batch.size` (default 10, advertised as `batch_credential_issuance.batch_size`), and the draft-13 and compatibility surfaces keep their token-bound, rotating nonces | consume at first check (breaks batches); never consume; cap at 1 | consume on success, cap 10 | agent (P3-03), pending owner review | `oid4vci-v1`, nonce cache |
| 2026-09-19 | A tenant with `issuer-did` issues under that DID: the core replaces the row's `did_url` with the tenant DID at signing time (verification method `<tenant did>#<kid>`) and the tenant's `/.well-known/did.json` publishes the deployment's keys under the tenant DID; per-tenant keys wait for `keyNamespace` support in the key providers | per-tenant rows with their own `did_url` only; per-tenant keys first | override at signing time, shared keys | agent (P2-07), pending owner review | core, tenancy, DID document |
| 2026-09-19 | `POST /issuance/credential` (develop body) carries `Deprecation`/`Link` headers, the counter and the kill switch from 1.1.0 (branch `wp/p1-11f-compat-credential-deprecation` merged on the owner's instruction); the remaining compatibility endpoints get theirs with the compat-core flip (reminder for the owner) | now; with the flip; never before 2.0.0 | now for the credential endpoint | project owner | compatibility surface, wallets that alert on `Deprecation` |
| 2026-09-19 | The path tenant resolver uses the fixed prefix `/t/{tenant}` under the servlet path, recognises only configured tenants (others fall back to the default tenant, as the host resolver does), derives `{deployment}/t/{tenant}/oid4vci` as the tenant's identifier unless `issuer-identifier` is set, and serves the metadata document only at the path-appended form `/t/{tenant}/oid4vci/.well-known/openid-credential-issuer` | configurable prefix; 404 for unknown tenants; host-root well-known form too | fixed prefix, default fallback, path-appended form | agent (P2-10), pending owner review | tenancy, `oid4vci-v1` |
| 2026-09-19 | The pre-authorized offer may name a `subject`; it becomes the access token's `sub` (as eSignet would set the individual id) so data providers that resolve records by `sub` work without eSignet; an offer with `claims` keeps the claims JSON in `sub` for `PreAuthDataProviderPlugin` | change the CSV plugin; always JSON in `sub`; a separate claim | `subject` in the offer | project owner (asked for CSV through the pre-authorized flow) | `certify-as`, data-provider plugins |
| 2026-09-19 | Pending: presentation during issuance follows an earlier draft (200 `status: require_interaction`, plain interaction types, `interactive_authorization_endpoint`, verifier `client_id` as audience); the 1.1 editor's draft moved to the authorization-challenge shape (403 `insufficient_authorization`, URN interaction types, `authorization_challenge_endpoint`, `ia_post`, audience = the endpoint). Keep today's shape for the Inji wallet and add the draft-current shape behind `certify.as.iae.*`? (docs/design/16-iae-audit.md) | replace; add next to it; leave | not taken | project owner | `certify-as`, wallets |
| 2026-09-19 | For the conformance run Certify's own AS gets an authorization code flow whose subject comes from `certify.as.authorization.subject-mode=fixed` (a configured subject, no user interaction; presentation during issuance and eSignet stay the production options), SD-JWT VC and mDoc chains come from the `x509-file` provider with a generated dev CA and leaf (HAIP forbids a self-signed signing certificate), and Token Status List rows reuse the existing status-list tables with a format column | see docs/design/17-conformance-gaps.md | fixed subject behind a flag, x509-file dev CA, reuse tables | project owner ("start", 2026-09-19) | `certify-as`, PKI, status lists |
| 2026-09-19 | A tasteful refactor pass over the legacy `certify-service` code, file by file, is owner-requested: goldens and vectors stay byte-identical, no abstraction without a stated need, each file's change justified in `docs/design/wp/R-00-refactor-charter.md` | none; big-bang rewrite | file-by-file behind the goldens | project owner (2026-09-19) | `certify-service` internals |
| 2026-09-19 | The chain a credential carries is a per-configuration choice (`signing_config.x5c`: full, leaf, without-anchor, none) with the formatter's default (the full keymanager chain) when absent, so existing output is unchanged and HAIP configurations opt into anchor-free chains; the chain policy runs for the PKI formats (SD-JWT VC, mDoc) only, since Data Integrity proofs resolve keys by DID | global switch; policy on every format | per configuration, PKI formats | agent (P3-08), pending owner review | signing, formatters |
| 2026-09-19 | Token Status Lists reuse `status_list_credential` and `status_list_available_indices` with `credential_type = TokenStatusList` and the signed list JWT as the document (no migration); every list is signed with one configured key (`certify.status.token-status-list.key-ref`, chain inclusion `without-anchor`); bit order is least significant bit first as the draft says; served at `/credentials/token-status-list/{id}` next to the Bitstring endpoint | a new table; per-list key; a shared endpoint | reuse, one key, own endpoint | agent (P3-09), pending owner review | status lists |
